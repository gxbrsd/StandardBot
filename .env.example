# Discord bot token from Developer Portal > Bot
DISCORD_TOKEN=your_bot_token_here

# Application ID from Developer Portal > General Information
CLIENT_ID=your_application_id_here

# Optional: server used for fast development command deployment
# Required only when running `npm run deploy` without --global
DEV_GUILD_ID=your_development_server_id_here
